# NO REPEATS, LOOP=5
#
#
#
# Pattern Start
NOP { V { all_pins_norm = v0; } W { all_pins_norm = scan_setup; } } 
NOP { V { all_pins_norm = v1; } W { all_pins_norm = scan_setup; } }
NOP { V { all_pins_norm = v2; } W { all_pins_norm = scan_setup; } }
NOP { V { all_pins_norm = v1; } W { all_pins_norm = scan_setup; } }
NOP { V { all_pins_norm = v2; } W { all_pins_norm = scan_setup; } }
NOP { V { all_pins_norm = v1; } W { all_pins_norm = scan_setup; } }
NOP { V { all_pins_norm = v2; } W { all_pins_norm = scan_setup; } }
NOP { V { all_pins_norm = v1; } W { all_pins_norm = scan_setup; } }
NOP { V { all_pins_norm = v2; } W { all_pins_norm = scan_setup; } }
NOP { V { all_pins_norm = v1; } W { all_pins_norm = scan_setup; } }
NOP { V { all_pins_norm = v2; } W { all_pins_norm = scan_setup; } }
EXIT { V { all_pins_norm = C0101010101; } W { all_pins_norm = scan_setup; } } 
# Pattern End
#
#
#
#
#
#